{
  "default":{
    "rootDirectory": "./src",
    "outputDirectory": "/dist",
    "configuration": {
      "base":"/config",
      "files":[
        "build.json"
      ],
      "tasks":{
        "dir":"/tasks",
        "default": ["scripts"]
      }
    }
  }
}